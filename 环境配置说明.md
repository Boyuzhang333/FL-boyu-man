# 🔧 联邦学习项目环境配置说明

## 📋 必须要求
**重要：Man和Boyu必须使用完全相同的环境配置！**

## 🚀 配置步骤

### 1. Python版本
- 使用 Python 3.10 或 3.11
- 建议使用虚拟环境（conda或venv）

### 2. 必需的包及版本
```bash
# 核心框架
flwr==1.23.0                    # Flower联邦学习框架
torch>=2.0.0                    # PyTorch
torchvision>=0.15.0             # 用于CIFAR-10
tqdm                            # 进度条

# 支持包
grpcio-health-checking==1.62.3  # gRPC健康检查
colorama==0.4.6                 # 终端颜色
protobuf==4.25.3               # 协议缓冲区（重要：版本必须兼容）
```

### 3. 安装命令（选择一个）

#### 选项A：使用pip
```bash
pip install flwr==1.23.0 torch torchvision tqdm grpcio-health-checking==1.62.3 colorama==0.4.6 protobuf==4.25.3
```

#### 选项B：使用conda（推荐）
```bash
conda create -n fl-miage python=3.11
conda activate fl-miage
pip install flwr==1.23.0 torch torchvision tqdm grpcio-health-checking==1.62.3 colorama==0.4.6 protobuf==4.25.3
```

### 4. 验证安装
运行以下命令验证环境配置成功：

```python
import flwr
import torch
import torchvision

print('✅ Flower版本:', flwr.__version__)
print('✅ PyTorch版本:', torch.__version__)
print('✅ Torchvision版本:', torchvision.__version__)
print('✅ 设备:', torch.device('cuda' if torch.cuda.is_available() else 'mps' if torch.backends.mps.is_available() else 'cpu'))
```

### 5. 测试数据加载
```python
import prepare_dataset
trainloader, valloader, testloader = prepare_dataset.get_data_loader(5, 0, data_split='iid')
print('✅ 数据加载成功')
print(f'训练集大小: {len(trainloader.dataset)}')
```

## ⚠️ 重要注意事项

1. **不要安装TensorFlow** - 会与Flower 1.23.0产生冲突
2. **确保protobuf版本为4.25.3** - 其他版本可能导致导入错误
3. **两人必须使用相同版本的包** - 确保实验结果一致性

## 🔧 平台差异
- **Mac**: 会自动使用MPS加速（如果可用）
- **Windows**: 会使用CUDA（如果可用）或CPU
- 这些差异不会影响实验结果的趋势和比较

